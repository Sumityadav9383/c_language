#include <stdio.h>


int main() {
    int password;
    char username[50];
    
    printf("Enter the username: ");
    scanf("%s", username);
    
    printf("Enter the password: ");
    scanf("%d",&password); // Use & to get the address of password
    
    // Corrected comparison logic
    if (username, "sumityadav" && password == 123454321) {
        printf("Login successful\n");  
    } else {  
        printf("Wrong credentials\n");
    }

    return 0;
}
